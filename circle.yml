machine:
  python:
    version: 2.7.11
dependencies:
  pre:
    - pip install mkdocs
test:
  override:
    - mkdocs build --clean --verbose
deployment:
 production:
   branch: master
   commands:
     - mkdocs gh-deploy